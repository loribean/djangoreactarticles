from django.urls import path
from . import views

urlpatterns = [
    #show one, show all, create, update, delete
    # path('articles/', views.ListTodo.as_view()),
    # path('<int:pk>/', views.DetailTodo.as_view()),
]